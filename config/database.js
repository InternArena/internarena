var config = {database: {}, secret: ''};

config.database = {
    host    :   '35.229.18.155',
    user    :   'root',
    password:   'nodejs',
    database:   'dbinternarena'
}
config.secret = 'yoursecret';

module.exports = config;
